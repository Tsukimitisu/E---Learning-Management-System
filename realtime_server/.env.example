NODE_ENV=production
PORT=3000
SOCKET_PATH=/socket.io

# Comma-separated origins, or * for all
CORS_ORIGIN=https://your-domain.com

# If running behind reverse proxy
TRUST_PROXY=1

# Socket heartbeat tuning (milliseconds)
PING_TIMEOUT_MS=30000
PING_INTERVAL_MS=25000
